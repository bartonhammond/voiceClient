enum GraphQLClientType {
  FileServer,
  Mp3Server,
  ApolloServer,
  ImageServer,
}

enum TabItem {
  stories,
  friends,
  messages,
  profile,
  newStory,
}

enum TypeUser {
  friends,
  users,
  me,
}

enum TypeSearch {
  date,
}

enum TypeStoriesView {
  allFriends,
  oneFriend,
}
