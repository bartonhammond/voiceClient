enum GraphQLClientType {
  FileServer,
  ApolloServer,
}
enum TabItem {
  stories,
  friends,
  messages,
  profile,
  newStory,
}

enum TypeUser {
  friends,
  users,
  me,
}

enum TypeSearch {
  date,
  hashtag,
}

enum TypeStoriesView {
  allFriends,
  oneFriend,
}
